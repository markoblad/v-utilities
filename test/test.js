'use strict';
var expect = require('chai').expect;
var index = require('../dist/index.js');
var VUtilities = index.VUtilities;

describe('VUtilities functions test', () => {
  it('should return ', () => {
    // var result = VUtilities.ALPHABET[0];
    // expect(result).to.equal('a');
  });
});
